__import__("selopt").runselfile(start="Choose a file to run:    ")
# 'ultra compact' is 1 line and 1020 characters
# and 'smart' should be unbeatable and implements a minimax algorithm (but without alpha beta pruning so it takes a few seconds to load)
# actually does have alpha beta pruning I think